

import './GradientColorLeft.css';

const GradientColorLeft = () => {
  return (
    <div 
      className="glowing-element1 absolute"
      style={{
        // top: top,  // Adjust as needed
        right: "auto",  // Adjust as needed
        bottom: "auto",  // Optional, adjust if needed
        left: "-16%",  // Optional, adjust if needed
      }}
    ></div>
  );
};

export default GradientColorLeft;
